version: "3"
services:
    backend:
        build:
            context: ./backend
            dockerfile: Dockerfile
        image: iddhi/library-system:backend

    frontend:
        build:
            context: ./frontend
            dockerfile: Dockerfile
        image: iddhi/library-system:frontend
